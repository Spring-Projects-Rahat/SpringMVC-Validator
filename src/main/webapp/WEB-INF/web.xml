<!-- webapp/WEB-INF/web.xml -->
<web-app xmlns="http://java.sun.com/xml/ns/javaee" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd"
    version="3.0">

    <display-name>To do List</display-name>
    
    <servlet>
            <servlet-name>appValidatorServlet</servlet-name>
            <servlet-class>
                org.springframework.web.servlet.DispatcherServlet
            </servlet-class>
    <!-- Below configuration can be used when you want to have different configuration file or to have on different location
            
            
    -->
    <init-param>
                <param-name>contextConfigLocation</param-name>
                <param-value>/WEB-INF/appValidator_servlet.xml</param-value>
            </init-param>
            <load-on-startup>1</load-on-startup>
        </servlet>

        <servlet-mapping>
            <servlet-name>appValidatorServlet</servlet-name>
            <url-pattern>/</url-pattern>
        </servlet-mapping>

      <!-- Use for applying springSecurityFilterChain 
       <filter>
                <filter-name>springSecurityFilterChain</filter-name>
                <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
       </filter>

       <filter-mapping>
            <filter-name>springSecurityFilterChain</filter-name>
            <url-pattern>/*</url-pattern>
       </filter-mapping>
        -->
        
	<error-page>
		<error-code>404</error-code>
		<location>/resources/404.jsp</location>
	</error-page>

</web-app>